# BlackOut CI/CD Environment Variables
# Copy this file to .env and fill in your values

# GPG signing key (base64-encoded private key)
# Generate: gpg --export-secret-keys --armor KEY_ID | base64 -w0
SIGNING_KEY=

# Optional: override GPG key password if set
# SIGNING_KEY_PASSWORD=
