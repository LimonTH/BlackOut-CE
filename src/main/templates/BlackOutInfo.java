package bodevelopment.client.blackout;

public sealed class BlackOutInfo permits BlackOut {
    protected static final String NAME = "${name}";
    protected static final String VERSION = "${version}";
}